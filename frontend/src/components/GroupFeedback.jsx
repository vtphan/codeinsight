const GroupFeedback = ({ onOpenModal }) => {
  return (
    <button 
      className="btn btn-primary"
      onClick={onOpenModal}
    >
      Group Feedback
    </button>
  );
};

export default GroupFeedback;
